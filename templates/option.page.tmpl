<!DOCTYPE html>
<html>
<head>
  <title>PDF Manager</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
      margin: 0;
      padding: 20px;
    }

    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #fff;
      border-radius: 4px;
      padding: 20px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    h1 {
      text-align: center;
      margin-top: 0;
    }

    .options {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 30px;
    }

    .option {
      text-align: center;
      padding: 20px;
      background-color: #007bff;
      color: #fff;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      margin-bottom: 10px;
    }

    .option:last-child {
      margin-bottom: 0;
    }

    .option:hover {
      background-color: #0056b3;
    }

    @media screen and (max-width: 768px) {
      .container {
        padding: 10px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>PDF Manager</h1>
    <div class="options">
      <div class="option" onclick="window.location.href='/upload-pdf'">Upload PDF</div>
      <div class="option" onclick="window.location.href='/get-pdf'">Get All PDFs</div>
    </div>
  </div>
</body>
</html>
